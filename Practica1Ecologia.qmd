---
title: "Práctica 1: Dinámicas para una población no estructurada por edad"
format: 
  revealjs:
    footer: "Dinámicas para una población no estructurada por edad"
    transition: slide
    smaller: true
    scrollable: true
---

##

1.  Una población tiene inicialmente 20 individuos y crece independientemente de su densidad durante 5 unidades de tiempo, pasando a estar integrada por 500 individuos. ¿Cuál será la diferencia entre las tasas de natalidad y mortalidad per cápita y por unidad de tiempo?

```{r echo=TRUE}
# Parámetros
N0 <- 20
Nt <- 500
t_final <- 5

# Calcular r
r <- log(Nt / N0) / t_final
r
```

::: {.callout-tip title="Modelo exponencial"}
$N(t)=N_{0}e^{rt}$
:::

## 

```{r echo=TRUE, fig.align='center', fig.height=6}
# Secuencia temporal
t <- seq(0, t_final, by = 0.1)
# Modelo exponencial
N <- N0 * exp(r * t)
# Gráfico
plot(t, N, type = "l", lwd = 2,
     xlab = "Tiempo",
     ylab = "Tamaño poblacional",
     main = "Crecimiento exponencial (densidad-independiente)")

```

## 

2.  Una población tiene inicialmente 1 millón de individuos de los cuales 20 están enfermos. Suponiendo que la enfermedad es contagiosa, que se extiende exponencialmente y que tras 5 semanas hay 500 individuos enfermos, determina el número de contagios per cápita y por unidad de tiempo

```{r echo=TRUE}
# Parámetros
N0 <- 20
Nt <- 500
t_final <- 5
```

## 

```{r echo=TRUE, fig.align='center'}
# Secuencia temporal
t <- seq(0, t_final, by = 0.1)
# Modelo exponencial
N <- N0 * exp(r * t)
# Gráfico
plot(t, N, type = "l", lwd = 2,
     xlab = "Tiempo",
     ylab = "Tamaño poblacional",
     main = "Crecimiento exponencial (densidad-independiente)")
```

## 

3.Obtén en una misma pantalla los gráficos de tamaño poblacional frente al tiempo para los siguientes valores de tasas intrínsecas de crecimiento: r = 0 i/(it); r = 0.1 i/(it) y r = -0.1 i/(it), siendo el tamaño inicial de la población 100 individuos y el período de crecimiento 20 unidades de tiempo. Indica para qué valores de “r” la población es creciente y para que valores es decreciente.

```{r echo=TRUE, fig.align='center'}
# Parámetros
N0 <- 100
t_final = 20
tiempo <- seq(0, t_final, by = 0.1)

r_vals <- c(0.1, 0, -0.1)

# Crear matriz para almacenar resultados
N <- sapply(r_vals, function(r) N0 * exp(r * tiempo))
head(N)
```

## 

```{r echo=TRUE, fig.align='center'}
# Graficar
matplot(tiempo, N, type = "l", lwd = 2, lty = 1,
        xlab = "Tiempo",
        ylab = "Tamaño poblacional",
        main = "Crecimiento exponencial para distintos valores de r")

legend("topleft",
       legend = c("r = 0.1", "r = 0", "r = -0.1"),
       col = 1:3,
       lwd = 2)
```

## 

4.  Para hacerte una idea de la enorme capacidad de crecimiento de una población cuando no se ve limitado por la densidad, calcula cuantos individuos habrá al cabo de 20 generaciones si se parte de 2 individuos y se sabe que el crecimiento por generación achacable a cada individuo es de 1.5 individuos.

```{r echo=TRUE}
# Parámetros
N0 <- 2
r <- 1.5
t_final <- 20
# Secuencia temporal
t <- seq(0, t_final, by = 0.1)
# Modelo exponencial
N <- N0 * exp(r * t)

```

## 

```{r}
# Gráfico
plot(t, N, type = "l", lwd = 2,
     xlab = "Tiempo",
     ylab = "Tamaño poblacional",
     main = "Crecimiento exponencial (densidad-independiente)")
```

## 

5.  Obtén el gráfico que describe el crecimiento discreto a lo largo de cuatro generaciones de una población que inicialmente contiene 50 individuos y en la que de una generación a otra aumenta un 50% el número de individuos ¿Cómo podría representarse la curva de crecimiento para ser más realista?

```{r echo=TRUE}
# Parámetros
N0 <- 50
lambda <- 1.5   # factor de crecimiento (50% más cada generación)
generaciones <- 0:4

# Modelo discreto
N <- N0 * lambda^generaciones

```

::: {.callout-tip title="Modelo discreto"}
$N(t)=N_{0}\lambda^{t}$
:::

## 

```{r echo=TRUE}
# Gráfico discreto
plot(generaciones, N,
     type = "b",        # puntos + líneas
     pch = 19,
     lwd = 2,
     xlab = "Generación",
     ylab = "Tamaño poblacional",
     main = "Crecimiento discreto (50% por generación)")
```

## 

6.  Obtén en un mismo gráfico las tres curvas de crecimiento discreto independiente de la densidad, al cabo de 20 generaciones, para valores de la tasa de crecimiento discreto de la población 1.1; 1.0 y 0.9, y un tamaño inicial de la población de 100 individuos. Establece el rango de valores de la tasa de crecimiento discreto de la población para los que la población es creciente o decreciente

```{r echo=TRUE}
# Parámetros
N0 <- 100
t_final = 20
tiempo <- seq(0, t_final, by = 1)

lambda_vals <- c(1.1, 1.0, 0.9)

# Crear matriz para almacenar resultados
N <- sapply(lambda_vals, function(lambda) N <- N0 * lambda^tiempo)
head(N)
```

## 

```{r echo=TRUE}
# Graficar
matplot(tiempo, N, type = "l", lwd = 2, lty = 1,
        xlab = "Tiempo",
        ylab = "Tamaño poblacional",
        main = "Crecimiento discreto independiente de densidad con distintos valores de lambda")

legend("topleft",
       legend = c(expression(lambda==1.1),
                  expression(lambda==1.0),
                  expression(lambda==0.9)),
       col = 1:3,
       lwd = 2)
```

## 

7.  Representa en un mismo gráfico el crecimiento de tres poblaciones durante 100 generaciones. Considera un tamaño inicial de 10 i, en un ambiente con capacidad de carga de 500 i, y unas tasas intrínsecas de crecimiento de 0.1; 0.4 y 1.5 i/(t i) ¿Qué conclusión general puedes sacar?

```{r echo=TRUE}
# Parámetros
N0 <- 10
K <- 500
r_vals <- c(0.1, 0.4, 1.5)
generaciones <- 0:100

# Función logística discreta
logistico <- function(r) {
  N <- numeric(length(generaciones))
  N[1] <- N0
  
  for (t in 1:(length(generaciones) - 1)) {
    N[t+1] <- N[t] + r * N[t] * (1 - N[t] / K)
  }
  
  return(N)
}

# Calcular las tres poblaciones
N_mat <- sapply(r_vals, logistico)
```

::: {.callout-tip title="Modelo logístico discreto"}
$N_{(t+1)}=N_t+rN_t(1-\frac{N_t}{K})$
:::

## 

```{r echo=TRUE, fig.align='center'}

# Graficar
matplot(generaciones, N_mat,
        type = "l",
        lwd = 2,
        lty = 1,
        col = 1:3,
        xlab = "Generación",
        ylab = "Tamaño poblacional",
        main = "Modelo logístico discreto (K = 500)")

abline(h = K, lty = 2)  # línea de capacidad de carga

legend("bottomright",
       legend = c("r = 0.1", "r = 0.4", "r = 1.5"),
       col = 1:3,
       lwd = 2)

```

## 

8.  En un hábitat con capacidad de carga de 300 i, ¿Cuántos individuos tendrá una población al cabo de 10 generaciones si su tasa intrínseca de crecimiento es 0.4 i/(it) y su tamaño inicial de 10 i? ¿Cuánto tiempo será necesario para alcanzar el punto de inflexión de la curva? ¿Qué representa el punto de inflexión?

```{r echo=TRUE}
# Parámetros
K <- 300
r <- 0.4
N0 <- 10
generaciones <- 0:10

# Vector para almacenar resultados
N <- numeric(length(generaciones))
N[1] <- N0

# Iteración del modelo logístico discreto
for (t in 1:(length(generaciones) - 1)) {
  N[t+1] <- N[t] + r * N[t] * (1 - N[t] / K)
}

# Mostrar tamaño tras 10 generaciones
N[11]

```

## 

```{r echo=TRUE}

# Gráfico
plot(generaciones, N,
     type = "b",
     pch = 19,
     lwd = 2,
     xlab = "Generación",
     ylab = "Tamaño poblacional",
     main = "Crecimiento logístico discreto")

abline(h = K, lty = 2)
which(N >= 150)[1] - 1
```

## 

9.  En la población anterior, se considera que la población ha alcanzado su capacidad de carga cuando llega a 290 individuos. El tiempo necesario para llegar a ese valor se le denomina “tiempo de retorno” y es el tiempo necesario para que se recupere el tamaño de una población después de una perturbación. Calcula el tiempo de retorno para las tres tasas intrínsecas de crecimiento per cápita indicadas en la tabla.

| Tasa intrínseca de crecimiento ($\frac{i}{it}$) | Tiempo de retorno ($t$) |
|:--------------------------------------------:|--------------------------|
|                       0.2                       |                         |
|                       0.4                       |                         |
|                       0.8                       |                         |

Cuando la tasa intrínseca de crecimiento se duplica. ¿Se reduce el tiempo de retorno a la mitad?

```{r echo=TRUE}
# Parámetros
K <- 290
N0 <- 10
N_retorno <- 290
r_vals <- c(0.2, 0.4, 0.8)

# Constante A del modelo logístico
A <- (K - N0) / N0

# Función para calcular tiempo de retorno
tiempo_retorno <- function(r) {
  ( -1 / r ) * log( (K / N_retorno - 1) / A )
}

# Aplicar a cada r
tiempos <- sapply(r_vals, tiempo_retorno)
tiempos

```

## 

10. Con los datos que aparecen por defecto en el modelo de crecimiento dependiente densidad en Populus obtén la curva dN/dt vs. N ¿Qué representa el máximo de la parábola? ¿y los puntos de corte con el eje “X”?

## 

11. Con los datos que aparecen por defecto en el modelo de crecimiento dependiente densidad en Populus obtén la gráfica dN/(Ndt) vs. N ¿Qué representa la gráfica dN/Ndt vs. N? ¿Qué representa el punto de corte con el eje “Y”? ¿y con el eje “X”?

## 

12. Representa la dinámica de una población con capacidad de carga 400 i, y tasa intrínseca de crecimiento per cápita r 0 = 0.1 i/(i t). Considera dos escenarios para el tamaño inicial de la población: N0 =5 y N0 =800. Anota en ambos escenarios el tamaño poblacional para los tiempos t de 5, 20 y 50 y el crecimiento poblacional.

| Tiempo | $N_t(N_0=5)$ | $N_t(N_0=800)$ | $\frac{dN}{dt}(N_0=5)$ | $\frac{dN}{dt}(N_0=5)$ |
|:-------------:|:-------------:|:-------------:|:-------------:|:-------------:|
| 5 |  |  |  |  |
| 20 |  |  |  |  |
| 50 |  |  |  |  |

En valor absoluto, ¿qué es mayor, el crecimiento poblacional cuando el número inicial de individuos es 5, o el decrecimiento poblacional cuando el número inicial de individuos es 800?.

## 

13. Considera una dinámica de crecimiento de una población en la que la tasa intrínseca de crecimiento (r 0 ) es 0.2 i/(it), el tamaño inicial de la población de 10 i y la capacidad de carga de 600 i. Supón que existe un retardo (τ) del efecto de los cambios de densidad poblacional sobre la tasa intrínseca de crecimiento per cápita (r 0) y que este retardo vale 1. Completa la tabla con los rangos que definen tres tipos de crecimiento considerando tres casos:


a)  Se mantiene ese τ, pero se van incrementando los valores de r 0
b)  Se mantiene esa r 0 , se van incrementando los valores de τ
c)  Se modifican ambos parámetros

|                         |          |
|-------------------------|:--------:|
| **Tipo de crecimiento** | $\tau=1$ |
| sin oscilación          |          |
| oscilación amortiguada  |          |
| oscilación cíclica      |          |
| **Tipo de crecimiento** | $r=0.2$  |
| sin oscilación          |          |
| oscilación amortiguada  |          |
| oscilación cíclica      |          |
| **Tipo de crecimiento** | $\tau r$ |
| sin oscilación          |          |
| oscilación amortiguada  |          |
| oscilación cíclica      |          |
